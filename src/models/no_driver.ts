/* eslint-disable @typescript-eslint/no-empty-interface */
import { IDriverSystem } from './driver_system';

/** A driver object that represents no driver. */
export interface INoDriver extends IDriverSystem {}
